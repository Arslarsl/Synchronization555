<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.dell1.synchronization.MainActivity">
    <Button
        android:id="@+id/LunchMapp"
        android:layout_width="400dp"
        android:layout_height="60dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="105dp"
        android:onClick="process"
        android:text="Launch Mapp" />

    <Button
        android:id="@+id/launchMarket"
        android:layout_width="400dp"
        android:layout_height="wrap_content"
        android:text="launch Market"
        android:layout_below="@+id/LaunchMapp"
        android:layout_alignParentStart="true"
        android:onClick="process"
        android:layout_marginTop="15dp" />

    <Button
        android:id="@+id/SendEmail"
        android:layout_width="400dp"
        android:layout_height="60dp"
        android:layout_marginTop="16dp"
        android:text="Send Email"
        android:layout_below="@+id/launchMarket"
        android:onClick="process"
        android:layout_alignParentStart="true" />

</RelativeLayout>
